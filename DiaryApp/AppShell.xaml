<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DiaryApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DiaryApp"
    xmlns:views="clr-namespace:DiaryApp.Views"
    Shell.FlyoutBehavior="Disabled">

	<TabBar>
		<!--<Tab Title="Sleep"
		 Icon="sleep.png">
			<ShellContent
                Title="Sleep Behaviour"
                ContentTemplate="{DataTemplate local:SleepBehaviourPage}"
                Route="SleepBehaviourPage" />
		</Tab>-->
		<Tab Title="Day"
		 Icon="day.png">
			<ShellContent
                Title="Day Behaviour"
                ContentTemplate="{DataTemplate views:DayBehaviourPage}"
                Route="DayBehaviourPage" />
		</Tab>

		<Tab Title="Show"
		 Icon="show.png">
			<ShellContent
                Title="Show Entries"
                ContentTemplate="{DataTemplate views:ShowEntriesPage}"
                Route="ShowEntriesPage" />
		</Tab>
	</TabBar>
</Shell>
